---
import TagChip from "./TagChip.astro";

const { tags } = Astro.props;
---

<div class="tags">
  {tags.map((tag: string) => <TagChip tagName={tag} />)}
</div>

<style>
  .tags {
    display: flex;
    flex-wrap: wrap;
  }
</style>
